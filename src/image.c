#include <stdio.h>
#include "image.h"

int cmd_build(int argc, char **argv)
{
    (void)argc;
    (void)argv;

    fprintf(stderr, "[mdock] build: not implemented yet (Milestone M1)\n");
    return 1;
}
