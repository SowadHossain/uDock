#include <stdio.h>
#include <time.h>

#include "timeutil.h"

int mdock_current_timestamp(char *buf, size_t size)
{
    (void)buf;
    (void)size;

    fprintf(stderr, "[mdock] time utilities not implemented yet\n");
    return -1;
}
